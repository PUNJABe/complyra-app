import { LoginScreen } from "@/components/app/login-screen";

export default function Home() {
  return <LoginScreen heading="Sign in to Complyra" description="Use the landing page to log in and enter your compliance workspace." />;
}
